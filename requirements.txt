# ClinicalPilot — DEPLOYMENT requirements (lean, free-tier friendly)
# Target Python: 3.11 (pinned in `.python-version` and render.yaml's PYTHON_VERSION).
# NOTE: do NOT hard-pin `openai` below litellm's floor — litellm>=1.75 needs openai>=1.68.2.
# Pinning openai==1.50.0 (the old file) is what caused Render's ResolutionImpossible.
# Heavy/optional extras (RAG vector store, advanced doc parsing, LangChain)
# live in requirements-optional.txt — they are NOT needed to run the app and
# every use of them in the code is lazy + guarded with a graceful fallback.

# Core Framework
fastapi>=0.116.0
uvicorn[standard]>=0.35.0
python-multipart>=0.0.20
python-dotenv>=1.1.0
pydantic>=2.11.0
pydantic-settings>=2.10.0
certifi

# LLM & Agents — LiteLLM is the unified client (Ollama/OpenAI/Groq/Azure/Anthropic/
# any OpenAI-compatible endpoint). It brings the OpenAI SDK; no provider-specific SDKs needed.
litellm>=1.75.0
openai>=1.99.0

# PHI Anonymization (Presidio + spaCy). The small model keeps memory low on free tiers;
# the anonymizer degrades to regex scrubbing if the model can't load.
presidio-analyzer>=2.2.360
presidio-anonymizer>=2.2.360
# spaCy capped to 3.8.x to match the en_core_web_sm-3.8.0 model wheel (models pin to a spaCy minor).
spacy>=3.8.0,<3.9.0
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# Document parsing (light) — PDF EHR upload. `unstructured` is optional (see optional file);
# ehr_parser falls back to PyPDF2 automatically.
PyPDF2>=3.0.1

# PubMed API (literature agent; optional at runtime — search is skipped if unavailable)
biopython>=1.85

# HTTP
httpx>=0.28.1
